import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.punjabfreighthub.app',
  appName: 'Warraich Goods',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
